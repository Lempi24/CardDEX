services:
  - type: web
    name: carddex-backend
    env: node
    region: frankfurt
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y chromium
      npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20
